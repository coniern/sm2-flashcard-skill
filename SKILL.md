---
# ---------------------------------------------------------------------------
# SKILL.md 是一个"技能"的说明书，也是 AI 助手读取这个技能时唯一必读的文件。
# frontmatter（--- 之间的部分）是给"技能市场/检索系统"看的元数据，
# 必须包含 name 和 description 两个字段；其余按需扩展。
#
# 为什么这样设计？
#   - name：唯一标识，约定俗成用 "动词短语" 或 "名词短语"，全小写+连字符
#   - description：第一句就要说清"什么时候用这个技能"，要足够具体，
#     让 AI 助手在茫茫技能库里能准确命中它。
# ---------------------------------------------------------------------------
name: sm2-flashcard-skill
version: 1.0.0
description: >
  基于 SM-2 间隔重复算法的记忆卡片技能：管理词卡/概念卡，自动计算每张卡片
  的下次复习时间。适用于背单词、备考（考研/法考/医考/教资）、记忆专业概念
  等任何需要长期记忆的场景。用户提到"复习""记忆""卡片""遗忘曲线""间隔重复"，
  或想记录/回顾自己学过的知识点时使用。提供 add/review/stats 三个命令，
  可直接在命令行运行，也可由 AI 助手调用。
metadata:
  requires:
    bins: ["python3"]        # 运行时依赖：只需 python3，无第三方库
  data:
    - data/deck.json         # 卡组文件
    - data/review_log.json   # 复习历史
---
# sm2-flashcard-skill —— 间隔重复记忆卡
> 一个**可以直接运行**的成熟 Skill 示例，同时是一份"如何写 Skill"的学习资料。
> 它把经典的 SM-2 间隔重复算法封装成：算法核心 + 命令行工具 + 数据文件 + 单元测试。
## 这个技能解决什么问题
人学了东西一定会忘，而 SM-2 算法能在"快要忘掉的那一刻"安排复习，
用最少的次数达到长期记忆（原理见 `references/SM2_algorithm.md`）。
本技能把整套流程封装成命令行工具，你可以：
- 随手加卡片（单词、概念、公式……）
- 每天花几分钟复习"今天到期的"卡片
- 查看学习统计，知道哪些卡快掌握了
## 何时使用（AI 助手触发条件）
- 用户想背单词 / 记忆概念 / 备考，提到"复习""记忆""卡片""遗忘曲线""间隔重复"
- 用户说"添加一张卡片""今天该复习什么""看看我的学习进度""我忘了哪些词"
## 快速开始（拿来就用）
```bash
cd sm2-flashcard-skill
# ① 查看内置示例卡组统计（内置 8 个英语词 + 8 个 408 概念卡）
python3 scripts/review.py stats
# ② 添加一张自己的卡片
python3 scripts/review.py add "时间复杂度" "描述算法运行时间随输入规模的增长趋势"
# ③ 开始复习（交互模式：先看正面，回车翻背面，自评 0~5）
python3 scripts/review.py review
# ④ 跑一遍单元测试，验证算法正确（无需安装任何依赖）
python3 tests/test_sm2.py
```
## 目录结构
```
sm2-flashcard-skill/
├── SKILL.md                  # 本文件：技能说明书（AI 助手必读）
├── scripts/
│   ├── sm2.py                # SM-2 算法核心（纯函数，最易测试）
│   └── review.py             # 命令行入口：add / review / stats
├── data/
│   ├── deck.json             # 卡组数据（示例卡组可直接用）
│   └── review_log.json       # 复习历史（自动生成）
├── references/
│   └── SM2_algorithm.md      # 算法原理文档（讲清"为什么"）
└── tests/
    └── test_sm2.py           # 单元测试（证明能跑、逻辑对）
```
## 核心设计：为什么这个例子能体现"成熟 Skill"
| 成熟 Skill 的特征 | 本项目的体现 |
|---|---|
| 有明确使用场景 | 所有"长期记忆"场景都适用，不只针对一种考试 |
| 逻辑有依据 | SM-2 是 SuperMemo/Anki 验证几十年的标准算法 |
| 能跑通、可验证 | 单元测试覆盖全部算法规则，clone 即可一键运行 |
| 拿来就用 | 内置真实卡组，add/review/stats 开箱即用 |
| 代码可读 | 常量命名、函数注释、数据结构字段说明齐全 |
| 数据安全 | 写入前自动备份 .bak，损坏时回退默认值不崩 |
| 可扩展 | 卡组/日志为纯 JSON，方便接界面、接 AI、换存储 |
## 常见坑（写 Skill 时的反面教材）
1. **description 写得太泛**（如"处理记忆"）→ AI 助手无法判断何时调用；
   应写成"提到 复习/记忆/卡片/遗忘曲线 时使用"。
2. **脚本依赖一堆第三方库** → 别人 clone 下来跑不起来；
   本项目只依赖 python3 标准库。
3. **没有测试** → 算法逻辑改动后没人敢保证还正确；
   本项目 11 个用例覆盖全部规则，改动即有反馈。
4. **数据写入不设防** → 中途出错可能丢数据；
   本项目每次保存前先备份 .bak。
5. **把算法和界面耦合在一起** → 难以单独测试；
   本项目把纯算法抽到 `sm2.py`，界面在 `review.py`，职责分离。
## 许可与作者
示例项目，公开可复用。作者：sm2-flashcard-skill 项目。
